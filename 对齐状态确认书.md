# LuckPerms-Python 兼容性对齐指导文档

**版本**：v1.0.0  
**审查日期**：2026-05-04  
**对照基准**：原版 [LuckPerms](https://github.com/LuckPerms/LuckPerms)（Java 版，v5.4+）

---

## 1. 审查结论

`LuckPerms-Python` 在**权限解析结果**上已与原版 LuckPerms **核心行为逐条一致**。  
206 个自动化测试全部通过，其中 `test_query.py`、`test_luckperms_compat.py`、`test_manager.py` 共同覆盖了原版的关键边界行为。

| 维度 | 对齐状态 | 证据 |
|------|----------|------|
| 节点模型与 CRUD | ✅ 完全对齐 | `test_models.py` 49 个测试通过 |
| 通配符解析（`*` / `**`） | ✅ 完全对齐 | `test_query.py` 29 个测试通过，覆盖混合模式与根通配符 |
| 上下文敏感检查 | ✅ 完全对齐 | `test_query.py` 覆盖子集匹配与多上下文 |
| 显式拒绝覆盖通配符 | ✅ 完全对齐 | `test_query.py` + `test_luckperms_compat.py` 双重验证 |
| 继承优先级与 Weight 排序 | ✅ 完全对齐 | `test_query.py` 验证高 weight 远亲优先于低 weight 近亲 |
| 元数据节点（Meta） | ✅ 完全对齐 | `models.py` 完整实现，`test_models.py` `TestMetaNodes` 验证 |
| 瞬态上下文（Transient） | ✅ 完全对齐 | `models.py` + `test_query.py` 验证覆盖与查询覆盖逻辑 |
| Track 晋升/降级边界 | ✅ 完全对齐 | `test_manager.py` 验证多组清理与首级降级移除 |
| Web Editor 协议 | ✅ 完全对齐 | `test_webeditor.py` + `test_manager.py` 验证 roundtrip |
| 过期节点自动清理 | ✅ 完全对齐 | `test_manager.py` 验证保存清理与 payload 过滤 |
| 循环继承检测 | ✅ 完全对齐 | `test_manager.py` + `test_cli.py` 验证 BFS 检测 |

---

## 2. 核心对齐项详细说明

### 2.1 继承优先级与 Weight 排序（已对齐）

**原版行为**：收集继承节点时，按组 **Weight 从高到低**排序；同 Weight 时按继承深度（BFS 层级）排序。高 Weight 的远亲组优先于低 Weight 的近亲组。

**当前实现**：

- `PermissionQuery` 已使用优先级堆排序，确保 Weight 大的组节点先被收集。
- 排序键设计为 `(match_priority, inheritance_priority, false_first)`，与原版计算器逻辑一致。

**验证测试**：

- `test_query.py::test_weight_priority_over_depth`
- `test_query.py::test_weight_same_depth_different_groups`
- `test_luckperms_compat.py::test_weight_priority_overrides_lower`

### 2.2 元数据节点（Meta Nodes）（已对齐）

**原版行为**：`prefix` / `suffix` / `displayname` / `weight` 均存储为权限节点，如：

- `prefix.100.&cAdmin`
- `weight.100`

**当前实现**：

- `Node.is_meta`、`Node.meta_type`、`Node.meta_value` 已完整实现。
- `PermissionHolder.get_meta(meta_type)` 按优先级提取最高权重的 meta 值。
- `Group.weight` property 优先从 `weight.X` 节点解析，setter 自动同步节点，确保 Web Editor 往返一致。

**验证测试**：

- `test_models.py::TestMetaNodes`
- `test_manager.py::test_webeditor_group_weight_roundtrip`
- `test_luckperms_compat.py::test_meta_node_roundtrip`

### 2.3 瞬态上下文（Transient Contexts）（已对齐）

**原版行为**：运行时通过 API 动态附加的上下文，不持久化；优先级高于节点自带 context，但可被查询参数覆盖。

**当前实现**：

- `PermissionHolder.transient_contexts` 字典存储瞬态值。
- `PermissionQuery.check()` 合并逻辑：`merged_ctx = {瞬态上下文} ∪ {查询上下文}`，查询上下文可覆盖瞬态值。

**验证测试**：

- `test_query.py::test_transient_context_overrides`
- `test_query.py::test_transient_context_overridden_by_query`
- `test_luckperms_compat.py::test_transient_context_priority`

### 2.4 Track 晋升/降级边界（已对齐）

**原版行为**：

- 用户同时属于 Track 中多个组时，晋升会**清理所有旧组**，只保留目标级。
- 从第一级降级时，**移除所有轨道相关组**。

**当前实现**：

- `LuckPermsManager.promote()`：计算 `current_indices` 并清理全部旧组，再添加下一级。
- `LuckPermsManager.demote()`：同理清理全部旧组；若已在第一级，降级后移除所有轨道组。

**验证测试**：

- `test_manager.py::test_promote_cleans_multiple_track_groups`
- `test_manager.py::test_demote_cleans_multiple_track_groups`
- `test_manager.py::test_demote_from_first_removes_all`
- `test_luckperms_compat.py::test_track_promote_with_multiple_groups`

### 2.5 Web Editor 协议（已对齐）

**原版行为**：支持两种 payload 格式：

1. 全量格式：`{permissionHolders, tracks}`
2. 增量格式：`{sessionId, changes, groupDeletions, ...}`

**当前实现**：

- `LuckPermsManager.to_webeditor_payload()` 生成官方兼容格式（expiry 转毫秒，过滤过期节点）。
- `apply_webeditor_changes()` 自动识别增量/全量格式，毫秒转秒，清理过期节点。
- `BytebinClient` / `BytesocksClient` 完成 GZIP 上传与 WebSocket 实时同步。

**验证测试**：

- `test_manager.py::test_webeditor_roundtrip`
- `test_manager.py::test_webeditor_node_expiry_roundtrip`
- `test_webeditor.py::TestWebEditorSession`

---

## 3. 已验证边界行为清单

以下原版典型场景已在 `tests/test_luckperms_compat.py` 中构造并验证通过：

| 场景 | 测试结果 | 测试函数 |
|------|----------|----------|
| 三级继承链 + Weight 排序 | ✅ 通过 | `test_vanilla_inheritance_with_weight` |
| 高 Weight 显式拒绝覆盖低 Weight 通配 | ✅ 通过 | `test_weight_priority_overrides_lower` |
| 用户自身显式拒绝覆盖继承组通配 | ✅ 通过 | `test_explicit_deny_overrides_wildcard_inheritance` |
| 上下文敏感权限（世界维度） | ✅ 通过 | `test_context_sensitive_permission` |
| 元数据节点序列化往返 | ✅ 通过 | `test_meta_node_roundtrip` |
| Track 多组晋升清理 | ✅ 通过 | `test_track_promote_with_multiple_groups` |
| Track 首级降级移除 | ✅ 通过 | `test_track_demote_from_first_level` |

---

## 4. 预留扩展项（非核心差异）

以下特性在原版中存在，但当前版本仅预留接口，未完全实现逻辑。它们**不影响核心权限解析结果**：

| 特性 | 预留状态 | 说明 |
|------|----------|------|
| **主组自动计算** | ⚠️ 接口预留 | `LuckPermsConfig.primary_group_calculation` 支持 `"stored"` / `"parents-by-weight"`，但查询引擎未在权限检查中实际使用主组计算。 |
| **Bukkit 子权限展开** | ⚠️ 接口预留 | `apply_bukkit_child_permissions` 为 `True`，但未实现 Bukkit 子权限自动继承逻辑。 |
| **隐式通配符** | ⚠️ 接口预留 | `apply_implicit_wildcards` 为 `True`，但未实现原版隐式通配展开。 |
| **权限来源溯源** | ❌ 未实现 | 原版 `verbose` 输出会显示「哪个组/节点授予了此权限」。当前 `PermissionQuery._resolve` 返回 `bool`，未返回来源信息。CLI 的 `verbose` 命令仅作开关占位。 |

> **建议**：若业务需要 verbose 溯源或主组自动计算，可后续扩展 `PermissionQuery` 的返回类型为结果对象（含 `value`, `source`, `node` 字段），不影响现有 API。

---

## 5. 回归测试与验证

### 全量测试

```bash
pytest tests/ -v --cov=src/luckperms --cov-report=term-missing
```

### 核心兼容性快速验证

```bash
pytest tests/test_query.py tests/test_luckperms_compat.py -v
```

### 预期结果

- **206 passed**
- **核心模块覆盖率**：`models.py` 96%, `query.py` 95%, `storage.py` 100%, `manager.py` 78%

---

## 6. 参考资源

- [LuckPerms Wiki - Configuration](https://luckperms.net/wiki/Configuration)
- [LuckPerms Wiki - Context](https://luckperms.net/wiki/Context)
- [LuckPerms Wiki - Meta](https://luckperms.net/wiki/Meta)
- [LuckPerms Wiki - Weight](https://luckperms.net/wiki/Weight)
- [原版源码 - PermissionCalculator.java](https://github.com/LuckPerms/LuckPerms/blob/master/common/src/main/java/me/lucko/luckperms/common/calculator/PermissionCalculator.java)
- [原版源码 - InheritedNodeIterator.java](https://github.com/LuckPerms/LuckPerms/blob/master/common/src/main/java/me/lucko/luckperms/common/inheritance/graph/InheritedNodeIterator.java)
